from .const import CONF_OVERRIDE_MQTT_URL as CONF_OVERRIDE_MQTT_URL, CONF_OVERRIDE_REST_URL as CONF_OVERRIDE_REST_URL, CONF_VERIFICATION_CODE as CONF_VERIFICATION_CODE, CONF_VERIFY_MQTT_CERTIFICATE as CONF_VERIFY_MQTT_CERTIFICATE, DOMAIN as DOMAIN, InstanceMode as InstanceMode
from .util import get_client_device_id as get_client_device_id
from _typeshed import Incomplete
from collections.abc import Mapping
from deebot_client.authentication import Authenticator
from deebot_client.const import UndefinedType as UndefinedType
from homeassistant.config_entries import ConfigFlow as ConfigFlow, ConfigFlowResult as ConfigFlowResult, SOURCE_REAUTH as SOURCE_REAUTH
from homeassistant.const import CONF_COUNTRY as CONF_COUNTRY, CONF_DEVICE_ID as CONF_DEVICE_ID, CONF_MODE as CONF_MODE, CONF_PASSWORD as CONF_PASSWORD, CONF_USERNAME as CONF_USERNAME
from homeassistant.core import HomeAssistant as HomeAssistant, callback as callback
from homeassistant.helpers import aiohttp_client as aiohttp_client, selector as selector
from homeassistant.helpers.typing import VolDictType as VolDictType
from homeassistant.util.ssl import get_default_no_verify_context as get_default_no_verify_context
from typing import Any, override

_LOGGER: Incomplete

def _validate_url(value: str, field_name: str, schema_list: set[str]) -> dict[str, str]: ...
async def _validate_input(hass: HomeAssistant, user_input: dict[str, Any], device_id: str, authenticator: Authenticator) -> dict[str, str]: ...
async def _validate_mqtt(hass: HomeAssistant, user_input: dict[str, Any], device_id: str, authenticator: Authenticator) -> dict[str, str]: ...

class EcovacsConfigFlow(ConfigFlow, domain=DOMAIN):
    VERSION: int
    MINOR_VERSION: int
    _mode: InstanceMode
    _input: dict[str, Any]
    _authenticator: Authenticator | None
    _device_id: str
    async def _async_set_input(self, user_input: dict[str, Any]) -> Authenticator: ...
    async def _async_teardown_authenticator(self) -> None: ...
    @callback
    @override
    def async_remove(self) -> None: ...
    async def _async_request_device_verification_code(self, authenticator: Authenticator) -> dict[str, str]: ...
    def _finish_flow(self) -> ConfigFlowResult: ...
    @override
    async def async_step_user(self, user_input: dict[str, Any] | None = None) -> ConfigFlowResult: ...
    def _show_auth_form(self, user_input: dict[str, Any] | None, errors: dict[str, str]) -> ConfigFlowResult: ...
    async def async_step_auth(self, user_input: dict[str, Any] | None = None) -> ConfigFlowResult: ...
    async def async_step_device_verification(self, user_input: dict[str, Any] | None = None) -> ConfigFlowResult: ...
    def _show_reauth_form(self, user_input: dict[str, Any] | None, errors: dict[str, str]) -> ConfigFlowResult: ...
    async def async_step_reauth(self, entry_data: Mapping[str, Any]) -> ConfigFlowResult: ...
    async def async_step_reauth_confirm(self, user_input: dict[str, Any] | None = None) -> ConfigFlowResult: ...
